# ==================== OpenAI Configuration ====================
# OpenAI API key (required for PuzzleAgent and LandmarkProcessor)
OPENAI_API_KEY=your-openai-api-key-here

# ==================== Docker Image Configuration ====================
# Docker image tag for Spring Boot backend (used in CI/CD)
# Default: local (for local development)
# In CI/CD, this will be set to build number or git commit SHA
IMAGE_TAG=local

# ==================== MongoDB Configuration ====================
# MongoDB connection URL (used by all services)
# In docker-compose network, use: mongodb://mongo-scavenger:27017
# For external access: mongodb://<host>:27017
MONGO_URL=mongodb://mongo-scavenger:27017

# MongoDB database name
MONGO_DB=scavengerhunt

# ==================== Flask Microservices Configuration ====================
# Flask host (0.0.0.0 to accept connections from all interfaces)
FLASK_HOST=0.0.0.0

# Flask port (internal container port, not the exposed port)
FLASK_PORT=5000
